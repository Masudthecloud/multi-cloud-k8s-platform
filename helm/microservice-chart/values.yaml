replicaCount: 2
image:
  repository: your-registry/fastapi-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: fastapi.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
